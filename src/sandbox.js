/* eslint-disable */
const { hasErr, hasNotErr, fileContains, appendToFileIfHasNot, sh, progress } = require('./helpers')
const os = require('os')
const axios = require('axios')
const { JSDOM } = require("jsdom")
/* eslint-enable */

const main = progress(async function main () {
  //
})

main()
